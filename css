body {
  font-family: Arial, sans-serif;
  padding: 15px;
}
button {
  background-color: #ff0000;
  color: white;
  border: none;
  padding: 10px;
  cursor: pointer;
}
#summary {
  margin-top: 15px;
}
